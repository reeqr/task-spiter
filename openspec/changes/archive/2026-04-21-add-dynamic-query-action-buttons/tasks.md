## 1. 配置模型与迁移

- [x] 1.1 扩展提示词配置类型，新增 `queryActions` 结构与默认动作集合
- [x] 1.2 实现旧配置迁移逻辑，将原固定模板映射到默认动作并补齐缺失字段
- [x] 1.3 在设置面板增加动作配置编辑项（文案、启用、排序、首轮模板、追问模板）并接入保存/恢复

## 2. 查询链路改造

- [x] 2.1 将查询 prompt 组装逻辑改为按 `actionId` 选择模板
- [x] 2.2 将术语查询与概念查询入口统一为动作驱动接口，移除固定“解释/出题角度”分支耦合
- [x] 2.3 为追问请求接入 `actionId` 上下文，保证 followup 使用对应动作模板

## 3. UI 渲染与会话状态

- [x] 3.1 术语项操作区改为按已启用动作动态渲染按钮并保持独立 loading 状态
- [x] 3.2 概念标题区改为按已启用动作动态渲染按钮并复用同一查询动作机制
- [x] 3.3 弹窗状态增加动作标识与动作名称，确保不同动作会话隔离且关闭后重置

## 4. 验证与回归

- [x] 4.1 验证至少三个动作并存时的展示顺序、点击行为与禁用行为
- [x] 4.2 验证旧本地配置升级后的默认动作回填与模板可用性
- [x] 4.3 验证流式返回、来源展示、错误提示与 Enter 追问行为无回归
