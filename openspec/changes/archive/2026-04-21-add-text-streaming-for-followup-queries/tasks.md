## 1. 流式请求层改造

- [x] 1.1 在 `src/utils/api.ts` 新增追问文本流式请求函数（解释/出题角度），支持增量回调
- [x] 1.2 增加流式解析逻辑（SSE/chunk 缓冲拼接），提取文本增量并在结束时返回完整结果
- [x] 1.3 为不支持流式的 provider/model 增加自动回退到非流式的兼容逻辑

## 2. 追问弹窗增量渲染

- [x] 2.1 在 `ConceptBreakdownPage` 追问发送时先插入占位 assistant 消息
- [x] 2.2 将解释追问链路接入流式回调，按增量更新末条 assistant 消息内容
- [x] 2.3 将出题角度追问链路接入流式回调，按增量更新末条 assistant 消息内容
- [x] 2.4 流结束与异常中断时正确收敛 sending 状态，并保留已生成文本

## 3. 回归与验证

- [x] 3.1 验证解释追问流式体验：逐字/分段追加、Enter 防重入、完成态正确
- [x] 3.2 验证出题角度追问流式体验：逐字/分段追加、会话隔离、完成态正确
- [x] 3.3 验证不支持流式时自动回退非流式，结果与交互可用
