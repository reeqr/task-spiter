## 1. 配置全局开关与联网参数

- [x] 1.1 增加一个全局联网搜索开关与参数结构（条数、域名过滤、时间过滤、内容粒度）
- [x] 1.2 在配置读取与持久化流程中接入上述字段，并补充默认值与边界校验
- [x] 1.3 在模型管理界面增加全局联网配置项，并确保关闭时不影响现有使用路径

## 2. 接入联网搜索请求链路

- [x] 2.1 在 `src/utils/api.ts` 中扩展请求体构造逻辑，全局开关开启时所有 AI 请求优先尝试注入 `tools.web_search`
- [x] 2.2 实现 provider 支持性判断策略（静态能力表 + 运行时探测），并缓存运行时不支持状态
- [x] 2.3 覆盖 `breakdownTask`、`breakdownConcept`、`consultAI`、`queryKnowledgeAI`、`queryExamAnglesAI` 全链路接入与透传
- [x] 2.4 在请求层实现失败降级策略（联网失败或不支持时自动回退普通请求）

## 3. 回答展示与可追溯来源

- [x] 3.1 扩展查询响应数据结构，支持返回来源链接与时间信息
- [x] 3.2 在问答模态框中展示来源信息，并与回答内容保持清晰关联
- [x] 3.3 当联网模式未获取有效来源时展示明确提示，避免误导

## 4. 验证与回归

- [x] 4.1 验证三条核心路径：全局开关关闭、联网成功、联网失败自动降级
- [x] 4.2 验证不同 provider 下兼容性：支持联网与不支持联网两类模型行为
- [x] 4.3 更新必要文档与使用说明，包含联网配置、限制与排障建议
