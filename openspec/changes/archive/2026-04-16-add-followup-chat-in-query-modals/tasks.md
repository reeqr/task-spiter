## 1. 扩展提示词与 API 追问能力

- [x] 1.1 在 `src/utils/promptConfig.ts` 增加解释追问与出题角度追问模板字段，并兼容历史缓存
- [x] 1.2 在 `src/utils/api.ts` 扩展查询方法，支持传入历史消息与当前追问内容
- [x] 1.3 为两类追问请求补充错误处理与统一返回格式，确保前端可稳定渲染会话流

## 2. 实现解释弹窗多轮追问

- [x] 2.1 在 `src/components/ConceptBreakdownPage.tsx` 重构解释弹窗状态为会话结构（messages/input/sending）
- [x] 2.2 在解释弹窗中新增追问输入区并实现 Enter 发送逻辑（发送中防重复）
- [x] 2.3 在解释弹窗关闭时清空会话历史与输入状态，确保下次打开为新会话

## 3. 实现出题角度弹窗多轮追问

- [x] 3.1 在 `src/components/ConceptBreakdownPage.tsx` 重构出题角度弹窗状态为独立会话结构
- [x] 3.2 在出题角度弹窗中新增追问输入区并实现 Enter 发送逻辑（发送中防重复）
- [x] 3.3 在出题角度弹窗关闭时清空会话历史与输入状态，确保与解释弹窗互不影响

## 4. 交互回归与稳定性校验

- [x] 4.1 验证解释与出题角度两类会话独立，连续追问不串上下文
- [x] 4.2 验证 Enter 发送、请求失败提示、发送中禁重复触发
- [x] 4.3 验证关闭即清空：关闭后再次打开仅保留新首轮结果
