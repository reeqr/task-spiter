## 1. 查询考点自动拆解流程

- [x] 1.1 在 `ConceptBreakdownPage.tsx` 中重构 `handleQueryKnowledge`，点击“考点”时先定位目标术语节点并判断是否已有专属考点
- [x] 1.2 当目标术语无专属考点时，复用术语拆解逻辑触发自动拆解，并将子结果写回 `children`
- [x] 1.3 自动拆解成功后仅使用该术语 `children.knowledgePoints` 打开考点模态框
- [x] 1.4 自动拆解失败时给出错误提示，并确保不展示无关旧内容

## 2. 查询状态与按钮交互反馈

- [x] 2.1 为术语“考点”按钮增加查询中状态（loading/disabled），避免重复触发
- [x] 2.2 将查询中的状态粒度绑定到当前术语项，避免影响其它术语按钮
- [x] 2.3 在查询成功、失败、无专属考点三种路径下补齐可区分的用户提示

## 3. 紧凑列表布局优化

- [x] 3.1 压缩 `TerminologyList.tsx` 的卡片内边距、项间距和无关换行，保证单行优先展示
- [x] 3.2 压缩 `KnowledgePointList.tsx` 的卡片内边距、项间距和分组留白，提升单页信息密度
- [x] 3.3 调整递归子列表容器的间距策略，减少层级展开后的大块空白
- [x] 3.4 验证小屏下可读性，必要时允许回退到双行展示

## 4. 验证与回归检查

- [x] 4.1 验证点击不同术语“考点”后弹窗内容随术语变化，不再出现“内容不变”感知
- [x] 4.2 验证未拆解术语点击“考点”会先自动拆解再展示专属考点
- [x] 4.3 验证无专属考点时展示“暂无相关考点”，且不默认回退全局考点
- [x] 4.4 运行 lint 并清理新增的未使用变量/导入
